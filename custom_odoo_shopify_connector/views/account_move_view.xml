<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_move_form_shopify_refund" model="ir.ui.view">
        <field name="name">account.move.form.shopify.refund</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_open_shopify_refund_wizard"
                        type="object"
                        string="Refund on Shopify"
                        class="btn-secondary"
                        invisible="move_type != 'out_refund' or state != 'posted' or payment_state != 'paid' or shopify_refunded"/>
            </xpath>
        </field>
    </record>
</odoo>

