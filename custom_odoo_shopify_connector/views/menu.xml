<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Legacy Shopify menu placeholders removed; functionality now exposed
         via the consolidated enterprise menu in views/shopify_menu.xml. -->

    <record id="action_shopify_sale_orders" model="ir.actions.act_window">
        <field name="name">Orders</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('shopify_order_id','!=',False)]</field>
    </record>

    <!-- Legacy Orders menu removed; Orders now available under the
         enterprise Orders menu in views/shopify_menu.xml. -->

</odoo>